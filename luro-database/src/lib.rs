/// Luro's database context. This itself just handles an abstraction for saving and loading data from whatever database it is using in the backend, depending on the feature selected. For now only Postgres and saving to disk (TOML) is supported.
pub struct LuroDatabase {
    
}